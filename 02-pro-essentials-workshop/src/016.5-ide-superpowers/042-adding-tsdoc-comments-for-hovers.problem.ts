/**
 * Adds two numbers together.
 * @example
 * myFunction(2, 3); // returns 5
 * // woooow, look at that hover! It shows the function signature and the description!
 */

const myFunction = (a: number, b: number) => {
  return a + b;
};

myFunction(2, 3);
myFunction(5, 10);
